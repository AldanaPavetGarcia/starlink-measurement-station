pydantic>=2.7,<3
pytest>=8.0
pytest-cov>=5.0

# paho-mqtt: cliente MQTT del mock_starlink (semana 3, ADR-09) — publica al broker
# Mosquitto con QoS 1 y LWT. Usa la API v2 (CallbackAPIVersion.VERSION2).
paho-mqtt>=2.1,<3

# Nota: NO se agrega grpcio acá. Semana 10 (src/acquisition/) extrae de la
# antena real vía el binario `grpcurl` (subprocess), no bindings grpcio
# compilados -- ver enmienda de ADR-01 en docs/05_ADR.md. grpcurl no es un
# paquete pip, se instala en Dockerfile.acquisition.