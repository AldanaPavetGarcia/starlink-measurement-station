"""
Backend API REST (semana 9, CLAUDE.md §1.1 / docs/07_API_REST.md).

Alcance de este módulo (mío, Starlink): GET /health, GET /metrics/starlink*,
GET /nodes*, POST /ingest/starlink. Los endpoints /metrics/env* y
POST /ingest/env son dominio de Fede (meteo) y no se montan acá todavía --
ver docs/PROGRESS.md "Coordinación pendiente con Fede".
"""
